// Публичный API сегмента shared/lib — утилиты импортируются только отсюда.
export { cn } from './utils';
export { formatPrice } from './format-price';
export { formatDate, formatTime } from './format-date';
