// У слоя shared нет единого публичного API — импорты идут по сегментам:
// @shared/ui, @shared/lib, @shared/config, @shared/api, @shared/store,
// @shared/types. Файл оставлен как маркер слоя.
export {};
