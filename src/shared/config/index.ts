// Публичный API сегмента shared/config.
export { USE_MOCKS, API_URL, WS_URL } from './env';
export {
  MIN_PREPARATION_MINUTES,
  MAX_CART_ITEM_QUANTITY,
  MOCK_DELAY_MS,
  MAX_STATS_RANGE_DAYS,
  CURRENCY,
  LOCALE,
} from './constants';
export { ROUTES, getRoleHomePath } from './routes';
